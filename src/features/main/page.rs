#[component]
fn MainPage() -> view {
    view! {
        div(class="flex flex-row space-x-2") {
            // request builder func
            // body for different requests func
            // response func
        }
    }
} 